<UserControl
    x:Class="moonlight_xbox_dx.StreaksBackground"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Canvas x:Name="StreakCanvas"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                SizeChanged="Canvas_SizeChanged" />
        <Border HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Opacity="1">
            <Border.Background>
                <AcrylicBrush  TintColor="#000" TintOpacity="0" TintLuminosityOpacity="0" />
            </Border.Background>
        </Border>

        <Border Background="#000005" Opacity="0.45" />

        <Grid Opacity="0.6">
            <Rectangle x:Name="VignetteOverlayHorizontal"
                       IsHitTestVisible="False"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                        <GradientStop Color="#FF000000" Offset="0.0"/>
                        <GradientStop Color="#00000000" Offset="0.25"/>
                        <GradientStop Color="#00000000" Offset="0.75"/>
                        <GradientStop Color="#FF000000" Offset="1.0"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle x:Name="VignetteOverlayVertical"
                       IsHitTestVisible="False"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#FF000000" Offset="0.0"/>
                        <GradientStop Color="#00000000" Offset="0.25"/>
                        <GradientStop Color="#00000000" Offset="0.75"/>
                        <GradientStop Color="#FF000000" Offset="1.0"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>
    </Grid>
</UserControl>
